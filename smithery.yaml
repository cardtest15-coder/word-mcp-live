# Smithery configuration file: https://smithery.ai/docs/build/project-config

startCommand:
  type: stdio
  configSchema:
    type: object
    description: No configuration options required
  commandFunction:
    |-
    (config) => ({command:'uv', args:['run','--directory','.',  'word_mcp_server']})
  exampleConfig: {}
